cascade_out/wavs/
cascade_out/*.wav
vox_out/*.wav
__pycache__/
*.pyc

# results/ is the permanent experiment archive -- ALWAYS tracked.
# Nothing under it may be ignored; see results/README.md.
!results/
!results/**

# damage_ab audio: source, watermarked and ~150 attacked WAVs, all regenerated
# on the cluster by embed_pairs.py + run_damage.py. Kept OUT of results/ on
# purpose -- everything under results/ is tracked, and audio does not belong in
# the archive. scp work/listen/ off Great Lakes to audition it.
damage_ab/work/
damage_ab/clips.json

# client production-pipeline audio (re-downloaded on the cluster from Dropbox)
client_processed/
client_stage_results.csv

# real-audio ablation experiment (re-downloaded on the cluster)
real_audio/
real_attacks_results*.csv
real_music_snr*.png

# reverb test outputs (regenerated on the cluster)
reverb_test_results*.csv
reverb_test*.png

# music-bed test outputs (regenerated on the cluster)
music_test_results.csv
music_test*.png

# demucs music-removal experiment (regenerated on the cluster)
demucs_work/
music_removal_results.csv
music_removal*.png

# stereo round-trip test (regenerated on the cluster)
stereo_work/
stereo_roundtrip_results.csv

# WPE dereverb test (regenerated on the cluster)
dereverb_wpe_results.csv
dereverb_wpe*.png

# client music-removal (regenerated on the cluster)
client_demucs/
client_music_removal.csv
client_correlation_all.csv
